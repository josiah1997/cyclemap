import React from "react";
import ReactDOM from "react-dom/client";
import CycleMap from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <CycleMap />
  </React.StrictMode>
);